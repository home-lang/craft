{
  "name": "test-app",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "ts-craft": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
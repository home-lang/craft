{
  "name": "craft-benchmarks",
  "version": "0.1.0",
  "description": "Hello World benchmarks: Craft vs Electron vs Tauri",
  "type": "module",
  "scripts": {
    "setup": "bun run setup.ts",
    "bench": "bun run run.ts",
    "bench:startup": "bun run startup.bench.ts",
    "bench:size": "bun run size.bench.ts",
    "bench:ipc": "bun run ipc.bench.ts",
    "bench:memory": "bun run memory.bench.ts"
  },
  "dependencies": {
    "mitata": "^1.0.10"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
